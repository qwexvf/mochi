# Node.js GraphQL Servers
FROM node:22-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy server files
COPY . .

# Expose port
EXPOSE 4000

# Run the specified server (SERVER env var set by docker-compose)
CMD ["sh", "-c", "node servers/${SERVER}.js"]
